apiVersion: v1
description: "Project for {{ .Release.Namespace }}"
displayName: {{ .Release.Namespace }}
kind: Namespace
metadata:
  name: {{ .Release.Namespace }}
  labels:
    opendatahub.io/dashboard: "true"
    name: {{ .Release.Namespace }}